using System;
using System.Collections.Generic;
using System.Text;

/// <summary>
/// 2.1.5 Als user wil ik dat mijn data wordt opgeslagen
/// 
/// D1 Connectie maken met de database en een node returnen (ConnectDb())
/// D2 Connectie maken met de database en een waarde returnen(ConnectDb2())
/// D3 Inloggen en connectie maken met Neo4j(CreateDriverWithBasicAuth())

/// </summary>
namespace XUnitTestMeldboek
{
    class DatabaseTests
    {
    }
}
